name: Deploy Nodes
on:
  push:
    branches:
      - main
jobs:
  deploy:
    concurrency: main
    runs-on: ubuntu-latest
    steps:
      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: xmtpeng
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Git Checkout
        uses: actions/checkout@v3

      - uses: actions/setup-go@v3
        with:
          go-version-file: go.mod

      - name: Deploy (dev)
        if: ${{ github.ref_name == 'main' }}
        env:
          TERRAFORM_TOKEN: ${{ secrets.TERRAFORM_TOKEN }}
        run: |
          export DOCKER_IMAGE_TAG=latest
          IMAGE_TO_DEPLOY=xmtp/node-go@$(dev/docker/build)
          echo Successfully pushed $IMAGE_TO_DEPLOY
          go run ./scripts/deploy/nodes --tf-token=$TERRAFORM_TOKEN --workspace=dev --container-image=$IMAGE_TO_DEPLOY --git-commit=$(git rev-parse HEAD)
          echo Successfully deployed

      - name: Deploy (production)
        if: ${{ github.ref_name == 'main' }}
        env:
          TERRAFORM_TOKEN: ${{ secrets.TERRAFORM_TOKEN }}
        run: |
          export DOCKER_IMAGE_TAG=production
          IMAGE_TO_DEPLOY=xmtp/node-go@$(dev/docker/build)
          echo Successfully pushed $IMAGE_TO_DEPLOY
          go run ./scripts/deploy/nodes --tf-token=$TERRAFORM_TOKEN --workspace=production --container-image=$IMAGE_TO_DEPLOY --git-commit=$(git rev-parse HEAD)
          echo Successfully deployed
