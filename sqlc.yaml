version: "2"
sql:
  - engine: "postgresql"
    queries: "pkg/identity/queries.sql"
    schema: "pkg/migrations/mls"
    database:
      uri: "postgres://postgres:xmtp@localhost:7654/postgres"
    gen:
      go:
        package: "queries"
        out: "pkg/identity/queries"
